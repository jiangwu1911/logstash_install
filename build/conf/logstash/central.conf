input {
    redis {
        host => "192.168.145.132"
        type => "redis-input"
        data_type => "list"
        key => "logstash"
    }
    syslog {
        port => 5514
        type => syslog
    }
}

output {
    stdout {}
    elasticsearch {
        cluster => "logstash"
    }
}
