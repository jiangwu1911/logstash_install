input {
    redis {
        host => 127.0.0.1        # REDIS_IP
        type => "redis-input"
        data_type => "list"
        key => "logstash"
    }
    syslog {
        port => 5514
        type => syslog
    }
}

output {
    elasticsearch {
        cluster => "logstash"
        host => "localhost"
        port => "9300"
    }
}
