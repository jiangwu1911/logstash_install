input {
    redis {
        host => "192.168.145.145"
        type => "redis-input"
        data_type => "list"
        key => "logstash"
    }
    syslog {
        port => 5514
        type => syslog
    }
}

output {
    elasticsearch {
        cluster => "logstash"
    }
}
